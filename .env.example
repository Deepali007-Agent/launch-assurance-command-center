# Optional publishing configuration. Core local validation requires no API key.
# These are examples only: set environment variables in your shell.
RETAIL_INTELLIGENCE_API_URL=
RETAIL_INTELLIGENCE_API_TOKEN=
# Optional override for the shared workflow database. Default: apps/retail/data.
# RETAIL_WORKFLOW_DB=/your/local/path/workflow.db

